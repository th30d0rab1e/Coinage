//setup new prices and stocks to database
    // get new RSI data
    //Aggregate Data
//RSI bitcoin

//Lowest aggregrate of day BUY. Highest, aggregate SELL. 

const db = require('./modules/database.js')
const ca = require('./modules/coinbaseAuth.js')

async function main() {
    try {
        //download price
    } catch (error) {
        console.log("main()", error)
    }
}
